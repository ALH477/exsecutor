# direnv is optional -- this repo does not require it. With direnv installed
# and enabled (`direnv allow`), this loads the flake's devShell automatically
# on `cd`. Without it, just run `nix develop` directly.
use flake
