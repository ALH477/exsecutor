# forma.exsc's header lists what each exit status means. It builds
# §5.2's DeModFrame from a struct literal, seals it with a CRC computed
# through `f sicut acies<u8, 17>`, checks all 17 bytes against the vendored
# example frame, and decodes it back. tests/unit/lwr_forma.asm pins the IR
# this file lowers to; this directory runs it.
TEST: expect-exit=0
